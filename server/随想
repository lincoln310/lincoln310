 
# 记录一些自己的想法，要一步步的实现
 * 使用模板+数据来构建动态网页。因为模板也是一种格式化的数据，所以都可以作为数据存储
 * 既然是作为数据，那么就可以存入数据库，从而做到不存在静态页面
 * 采用datomic的历史状态记录，可以随时切换版本。因为各个版本都已存入和数据库，可以在旧版本运行时，保持其时间点不更新，就把新版本的数据加入，并在测试通过后，更新运营版本时间戳，而从瞬间更新。
 
